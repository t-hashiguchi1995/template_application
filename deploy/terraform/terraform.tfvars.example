# コピーして terraform.tfvars にリネームし、実際の値を設定してください
# 例: cp terraform.tfvars.example terraform.tfvars
# 環境変数でも指定可能: export TF_VAR_project_id=your-actual-project-id

project_id             = "your-gcp-project-id"
region                 = "asia-northeast1"
artifact_registry_repo  = "template-app-repo"

# Cloud Run を専用 SA で動かし、GCS を使う場合（任意）
# cloud_run_sa_id   = "template-app-sa"
# gcs_bucket_name   = "template-app-your-gcp-project-id"   # グローバルで一意な名前
